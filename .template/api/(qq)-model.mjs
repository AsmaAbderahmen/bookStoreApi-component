import mongoose from 'mongoose';
const Schema = mongoose.Schema;


const (;qq;)_schema = Schema({

},{
    timestamps : true ,  usePushEach: true
});
export const (;
QQ;
)
= mongoose.model('(QQ)', (qq);
_schema;
)

